# Archive

Documentos que descrevem a **primeira implementação** da landing page — construída sem seguir
`docs/spec/` (que já existia mas não foi consultado). Essa implementação tinha estrutura de
componentes, localização de conteúdo e escopo diferentes do que foi especificado (ex: incluía
Dashboard/Reports e formulário de lead, que não fazem parte da spec original).

Mantidos aqui por histórico, não como documentação ativa. Para o que está valendo agora, ver
`docs/spec/`.
